; -*- mode: clojure; -*-
; vim: filetype=clojure

{:pg
 ;; Your Postgres DSN
 {:dbtype "postgresql"
  :dbname "ahungry"
  :host "localhost"
  :user "ahungry"
  :password "ahungry"
  :port 5432
  :ssl false
  ;; :sslfactory "org.postgresql.ssl.NonValidatingFactory"
  }
 ;; A sample custom query - find results in /custom
 ;; TODO: Change this from a single custom to "customs" and allow many
 :pg-custom
 {:sql "SELECT code FROM project LIMIT 1"
  ;; Or you could have :sql "/path/to/file.sql"
  :pk :code}}
